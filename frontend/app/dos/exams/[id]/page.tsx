// The Director of Studies uses the school admin's page (links stay in /dos —
// see lib/portal.ts; the server scopes access — see backend shared/dos.ts).
export { default } from '@/app/school-admin/exams/[id]/page';
