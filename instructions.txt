SCHOOL-OS DEPLOYMENT — BACKEND ON DIGITALOCEAN, FRONTEND ON VERCEL
====================================================================

ARCHITECTURE
------------
- Backend + Postgres + Redis run on the DigitalOcean droplet via
  docker-compose.prod.yml.
- The droplet's existing system Nginx (already serving other projects)
  gets one more vhost for sos.dementa.space, proxying to the backend
  container over localhost. Nginx keeps handling your other sites
  untouched.
- Frontend (Next.js) deploys to Vercel. It talks to the backend through
  Next.js rewrites (next.config.ts), so the browser only ever calls the
  Vercel domain — no CORS setup needed, and the backend's auth cookie
  rides back through the proxy onto the Vercel domain.


STEP 1 — DROPLET: SWAP (fixes slow/hanging docker builds on 1-2GB RAM)
------------------------------------------------------------------
    sudo fallocate -l 2G /swapfile
    sudo chmod 600 /swapfile
    sudo mkswap /swapfile
    sudo swapon /swapfile
    echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

Build images one at a time to avoid both Node builds competing for RAM
at once:
    docker compose -f docker-compose.prod.yml build backend


STEP 2 — DNS
------------
Subdomain sos.dementa.space already created, pointing an A record at
the droplet's public IP. (Done.)


STEP 3 — NGINX VHOST FOR THE SUBDOMAIN (does not touch other sites)
------------------------------------------------------------------
The repo includes deploy/nginx/sos.dementa.space.conf — a plain HTTP
vhost that proxies to the backend container on 127.0.0.1:4000.

On the droplet:
    sudo cp deploy/nginx/sos.dementa.space.conf /etc/nginx/sites-available/
    sudo ln -s /etc/nginx/sites-available/sos.dementa.space.conf /etc/nginx/sites-enabled/
    sudo nginx -t && sudo systemctl reload nginx

Get a TLS cert for just this subdomain (certbot edits only this vhost
file, leaves your other sites' certs alone):
    sudo apt install certbot python3-certbot-nginx   # if not already installed
    sudo certbot --nginx -d sos.dementa.space


STEP 4 — ENV FILE ON THE DROPLET
---------------------------------
Copy .env.example to .env in the repo root and fill in real values:
    JWT_SECRET=<openssl rand -hex 32>
    COOKIE_SECRET=<openssl rand -hex 32>
    POSTGRES_PASSWORD=<openssl rand -hex 32>
    COOKIE_SECURE=true

Do NOT reuse the dev-only fallback values from docker-compose.yml.


STEP 5 — START THE BACKEND STACK
----------------------------------
    docker compose -f docker-compose.prod.yml up -d --build

This starts postgres, redis, and backend. Only the backend is exposed,
and only to 127.0.0.1:4000 (loopback) — Nginx on the host is the only
thing internet-facing, same as before.

Verify:
    docker compose -f docker-compose.prod.yml logs -f backend
    curl https://sos.dementa.space   # should get a response from the API


STEP 6 — FRONTEND ON VERCEL
------------------------------
1. Import the repo into Vercel, set the project root directory to
   "frontend/" (the Dockerfile there is ignored by Vercel — Vercel
   builds Next.js natively).
2. Set environment variables in the Vercel project:
     BACKEND_URL=https://sos.dementa.space
     JWT_SECRET=<same value as on the droplet — must match exactly,
                 since the frontend verifies the same JWT the backend
                 signs>
     NEXT_PUBLIC_BACKEND_URL=https://sos.dementa.space
                 Same host as BACKEND_URL, but NEXT_PUBLIC_ so it ships to
                 the browser — needed by the communications module's
                 realtime (WebSocket) feature, which connects to the
                 backend directly instead of through the rewrite proxy
                 (see frontend/lib/realtime/useConversationSocket.ts).
                 Without it, live message delivery silently falls back to
                 trying localhost:4000 and just never connects; everything
                 else in the app is unaffected.
3. Deploy.


STEP 7 — VERIFY END TO END
-----------------------------
1. Open the Vercel URL, log in.
2. Confirm the auth cookie gets set (check devtools > Application >
   Cookies — should show on the Vercel domain, HttpOnly, Secure).
3. Confirm subsequent API calls succeed (Network tab, /api/v1/* calls
   returning 200s, not CORS errors).

If you ever add a direct client-side fetch straight to
sos.dementa.space instead of going through /api/v1/* on the frontend's
own origin, you will need CORS + sameSite:"none" on the backend cookie
— avoid that; keep everything routed through the Next.js rewrite.


FILES INVOLVED
----------------
docker-compose.yml           — local dev (unchanged)
docker-compose.prod.yml      — droplet production stack (postgres,
                                redis, backend only — no frontend, no
                                Caddy; Nginx on the host handles TLS)
deploy/nginx/sos.dementa.space.conf — Nginx vhost config, reference
                                       copy to install on the droplet
.env.example                 — template for .env, includes
                                POSTGRES_PASSWORD needed by
                                docker-compose.prod.yml


TROUBLESHOOTING — DROPLET UNREACHABLE VIA SSH AFTER POWER-OFF/PAYMENT ISSUE
----------------------------------------------------------------------------
If the droplet was powered off (e.g. billing hold) and comes back with
SSH key auth failing ("Permission denied (publickey)"), the disk usually
survives but authorized_keys / sshd state can be reset.

1. Use the DigitalOcean dashboard's web-based Droplet Console (Droplet >
   Access > Launch Droplet Console) to get a shell without SSH.

2. Authorize this machine's deploy key:
    mkdir -p ~/.ssh && chmod 700 ~/.ssh && echo 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICbMB/PX7RyiW6tafX6gSsDSVTEilgNPYwRLZKf82kws school-os-deploy' >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys && echo DONE

3. If SSH still refuses the key afterward, diagnose in the console with:
    whoami && echo "--- authorized_keys ---" && cat ~/.ssh/authorized_keys && echo "--- sshd config ---" && grep -Ei "PermitRootLogin|PubkeyAuthentication|AuthorizedKeysFile" /etc/ssh/sshd_config

   Checks:
   - whoami must be root (key was appended to the right home dir)
   - authorized_keys must contain the exact ssh-ed25519 key above
   - sshd_config must have PermitRootLogin yes (or prohibit-password)
     and PubkeyAuthentication yes
   - If AuthorizedKeysFile points somewhere non-default, put the key
     there instead, then: sudo systemctl restart sshd

4. Once SSH access is restored, confirm the stack survived the outage:
    docker ps -a
    docker compose -f docker-compose.prod.yml ps

   If containers are gone (not just stopped), re-run STEP 5 above to
   rebuild and start the stack — the postgres-data named volume should
   still be intact on disk even if the containers themselves were
   removed, so data is not necessarily lost.


DEMO DATA — MIREMBE HILL NURSERY & PRIMARY SCHOOL (FOR PRESENTATIONS)
=====================================================================
A made-up school with Nursery (Baby / Middle / Top Class) and P1–P7:
1,000 pupils in streams, 38 staff, the Uganda primary subjects, marked
and published exams, a full timetable for the current term, schemes of work, lesson
plans, attendance, parents and a term calendar.

Script:  backend/scripts/seed-demo-primary-school.ts
Content: backend/scripts/demo-data/uganda-primary-curriculum.ts


STEP 1 — GET THE SCRIPT ONTO main
---------------------------------
deploy.sh deploys origin/main only, so the two files above (and the
"seed-demo-primary-school" line in backend/package.json) must be
committed and merged to main first.


STEP 2 — DEPLOY (the school is created automatically)
------------------------------------------------------
    cd ~/projects/school-os
    ./deploy/deploy.sh

Nothing extra to run. After the normal deploy (pull, database backup,
rebuild, restart), deploy.sh looks for the demo school:
  - first deploy: not found, so it creates it (about a minute) and
    prints the logins;
  - every deploy after that: found, so it prints "already there —
    skipped" and leaves it untouched.

If creating it ever fails, nothing from it is kept, the deploy itself
still succeeds, and the next deploy tries again.


STEP 3 — OPTIONAL, BEFORE A PITCH: REFRESH THE DATES
----------------------------------------------------
The demo is built around the day it was created or last refreshed, so
over time it goes stale (the live exam closes, attendance and lesson plans stop at that
week, the term ends). Before each pitch — the same day, or the day
before — run on the droplet:

    cd ~/projects/school-os
    ./deploy/deploy.sh --demo-only

That backs up the database, then refreshes the demo school on the
running app — no rebuild, no restart, about 30 seconds.

To prepare the day before, give the pitch date:

    ./deploy/deploy.sh --demo-only --demo-date=2027-03-10

Deploys never refresh it — only --demo-only does.

What a refresh KEEPS: the school, staff, pupils, parents, classes,
subjects, and every login and password.

What a refresh REBUILDS around the pitch date: the school year and term
dates, exams and marks, attendance, schemes of work, timetable, lesson
plans, nursery progress ratings and the calendar. Anything of that kind
added by hand during an earlier pitch (marks entered live, a register
taken, events) is replaced — that's what keeps each pitch clean.

Wherever the pitch date falls, there is always a term in progress and
one exam open for live marks entry:
  - weeks 1–2 of a term ..... Beginning of Term exam is live
  - after that .............. Beginning of Term marked; Mid-Term live
  - End of Term exam week ... BOT and Mid-Term marked; End of Term live
  - holidays after Term 3 ... all marked; a Holiday Revision Test is live
If the pitch falls in a school holiday, the nearest term is stretched
to cover it.

One limit: in the first two weeks of Term 1 there are no published
results yet (the year has just started), so report cards will be
empty. Everything else still works. For report cards in a pitch then,
use --demo-date with a later day in the term.


STEP 4 — LOG IN AND CHECK
-------------------------
Password for EVERY demo account:  TestPass!2026

    School admin ........ admin@mirembehill.dev
    Head teacher ........ margaret.nankya@mirembehill.dev
    Director of Studies . paul.kasozi@mirembehill.dev   (DOS portal: /dos)
    Nursery teacher ..... harriet.nabukenya@mirembehill.dev  (Baby Class Red)
    P1 class teacher .... agnes.nansubuga@mirembehill.dev    (P1 Red)
    P7 English teacher .. denis.opolot@mirembehill.dev
    Parent .............. parent.demo@mirembehill.dev
                          (Sarah Mukasa — children in Top Class, P3, P7)
    Other teachers ...... firstname.lastname@mirembehill.dev
    Pupils .............. their S-number (e.g. S26xxxx)

The admin sees one section at a time — use the Nursery / Primary switch
to move between them.


WHAT TO SHOW IN THE PRESENTATION
--------------------------------
- Report cards: past terms' End of Term exams (and this term's
  Beginning of Term exam, from week 3) are marked and published (PLE
  aggregates and divisions for Primary, letter grades + progress
  ratings for Nursery).
- Live marks entry: one exam (usually the Mid-Term) is open THIS WEEK.
  About half the mark sheets are submitted, some are half done, some
  are empty — log in as a teacher (e.g. denis.opolot) and enter marks
  live.
- Timetable: the current term, every stream full, no teacher clashes.
- Lesson prep (DOS): schemes of work for every class and subject
  (NCDC topics), records of work, and this week's lesson plans —
  some approved, some submitted, some returned, some drafts.
- Attendance: taken every school day of the term up to yesterday.
  Today is left empty so you can take a register live.
- Parent portal: log in as parent.demo to see three children.
- Calendar: Independence Day, sports day, PLE dates, speech day, etc.

NOTE: "this week" and "yesterday" are worked out from the day you run
the deploy that created it, or --demo-only (or --demo-date). See STEP 3.

This school stays on production permanently — it is the school used for
pitching to clients. Do NOT delete it.
