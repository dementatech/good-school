# Reference material only — the ported source app. Not part of this repo.
inspo/

# Local env
.env
.env.local
.env.*.local

# Local test credentials — never commit real or dev logins
test.txt

# OS / editor
.DS_Store
